
<form onSubmit={handleSubmit(onSubmit)}>
    <input {...register("firstName")} />
    <select {...register("gender")}>
      <option value="female">female</option>
      <option value="male">male</option>
      <option value="other">other</option>
    </select>
    <input type="submit" />
  </form>









const AddClass = () => {
    const { register, handleSubmit, errors } = useForm();

    const handleAddClass = (data) => {
        console.log(data);
    }

    return (
        <div>
         <form onSubmit={handleSubmit(handleAddClass)}>
      <div>
        <label>Class Name:</label>
        <input type="text" name="class" {...register('class')} />
        {errors.class && <span>This field is required</span>}
      </div>

      <div>
        <label>Image:</label>
        <input type="file" name="image" {...register('image')} />
        {errors.image && <span>This field is required</span>}
      </div>

      <div>
        <label>Instructor Name:</label>
        <input type="text" name="instructorName" {...register('instructorName')} />
        {errors.instructorName && <span>This field is required</span>}
      </div>

      <div>
        <label>Available Seat:</label>
        <input type="number" name="availableSeat" {...register('availableSeat')} />
        {errors.availableSeat && <span>This field is required</span>}
      </div>

      <div>
        <label>Price:</label>
        <input type="number" name="price" {...register('price')} />
        {errors.price && <span>This field is required</span>}
      </div>

      <div>
        <label>Category:</label>
        <select name="category" {...register('category')}>
          <option value="popular">Popular</option>
          <option value="normal">Normal</option>
        </select>
        {errors.category && <span>This field is required</span>}
      </div>

      <button type="submit">Add Class</button>
    </form>        
        </div>
    );
};

export default AddClass;
